import React, { Component } from "react";
import PropTypes from "prop-types";

export class Card extends Component {
  render() {
    return <div />;
  }
}

export default Card({ handleClick, id, flipped, back, front, height, width });
